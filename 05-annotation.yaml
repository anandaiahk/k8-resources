apiVersion: v1
kind: Pod
metadata:
   name: annotation
   labels:
     project: roboshop
     component: frontend
     env: dev
   annotations:
    imageregistry: "https://hub.docker.com/"  
spec:
   containers:
   - name: nginx
     image: nginx 